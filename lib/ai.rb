class AI < Player
  def place_ships(number)
    # select random starting position for each ship
    # once start has been found choose a direction
    # count until we can report the ships full position
    # make sure we don't go off the board
    # do number times and increase length each times
  end
end
